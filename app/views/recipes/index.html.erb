<h1> Recipe Box </h1>

<% @recipes.each do |r| %>
	<div>
		<strong><%= r.name %></strong></br>
		Serves: <%= r.serves %>
		<% if r.serves.to_i != r.serves_max %>
			- <%= r.serves_max %>
		<% end %></br>
		Total Time: <%= r.total_time %>  minutes</br>
		Ingredients: <% r.ingredients.each do |i|%></br>
			<%= r.ingredient_recipes.find_by(ingredient_id: i).quantity %>
			<%= i.ingredient_name %>
			<% end %>
	</div></br>

	<% end %>
<h1> Recipe Box </h1>

<% @recipes.each do |r| %>
	<div>
		<strong><%= r.name %></strong></br>
		Serves: <%= r.serves %>
		<% if r.serves != r.serves_max %>
		- <%= r.serves_max %>
		<% end %></br>
		Total Time: <%= r.total_time %>  minutes</br>
	</div></br>

	<% end %>
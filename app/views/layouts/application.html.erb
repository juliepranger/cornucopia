<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %>Corncucopia</title>

    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <div id="user-widget">
    <!-- Collect the nav links, forms, and other content for toggling -->
    <ul class="navbar" role="navigation">
      <% if current_user %>
        <li id="navbar-image"><%= image_tag current_user.image %><li>
        <li><p>Welcome <strong><%= current_user.name %></strong>!</p></li>
        <li class="navbar-left"><%= link_to "Users",users_path %></li>
        <li class="navbar-left"><%= link_to "Parties",parties_path %></li>
        <li class="navbar-left"><%= link_to "Sign out", signout_path, id: "sign_out" %></li>
        <li class="navbar-left"><%= link_to "Cornucopia",root_path%></li>
      <% else %>
        <li class="navbar-left"><%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %></li>
      <% end %>
    </ul>
  </div>

    <%= yield %>
 
  </body>
</html>


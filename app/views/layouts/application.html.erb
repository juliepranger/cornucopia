<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta property="og:type" content="recipe" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href='http://fonts.googleapis.com/css?family=IM+Fell+English' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Nothing+You+Could+Do' rel='stylesheet' type='text/css'>
  <title>Cornucopia</title>

  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>

  <body>
    <header>
      <script>
      $(document).ready(function() {
        $('body').css('margin',0)
        $(window).scroll(function(){
          var top = $(window).scrollTop()
          if(top>262){
            $('#user-widget.navbar-header').css('opacity', 0);
          } 
          else if(top<5){
            $('#user-widget.navbar-header').css('opacity', 1);
          }
          else {
            $('#user-widget.navbar-header').css('opacity', 0.8);
          }
        });
      });
      </script>
      <div class="navbar-header" id="user-widget">
        <!-- Collect the nav links, forms, and other content for toggling -->
          <ul class="navbar-items">
            <li class="nav"><%= link_to "Cornucopia", root_path %></li>
            <li class="nav"><%= link_to "Recipes", recipes_path %></li>
          <% if current_user %>
            <li class="nav" id="welcome">Welcome <%= current_user.name %>!</li>
            <li class="nav"><%= link_to "My Parties", parties_path %></li>
            <li class="nav"><%= link_to "Sign out", "/logout" %></li>
          <% else %>
            <li class="nav-right"><%= render 'authentications/signin'%></li>
            <li class="nav-right"><%= link_to "Sign Up", "/users/new" %></li>
          <% end %>
        </ul>
      </div>
      <div id="header-paragraph">
      <h1 id="fix">Cornucopia</h1>
        <p>
        Because </br>
        potlucks are supposed to be easy, </br>
        planning with friends should be fun, </br>  headaches are not invited to parties, </br>
        & how you celebrate community matters. </br>
        We are here to help. </br>
        </p>
      </div>
  </header>

    <%= yield %>

  </body>
</html>

